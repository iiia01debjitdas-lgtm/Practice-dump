/*
let age = prompt("Enter your age!")
if ((age >= 14) && (age <= 90)) {
    alert("You are in the correct age group!")
} else {
    alert("You should not use a computer!")
} */

//NOW OPPOSITE//

let age = prompt("Enter your age!")
if (! (age >= 14) && (age <= 90)) {
    alert("You are in the correct age group!")
} else {
    alert("You should not use a computer!")
}